services:
  localstack:
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    environment:
      LAMBDA_DOCKER_NETWORK: workshop-lambda
    networks:
      - lambda
networks:
  lambda:
    name: workshop-lambda
